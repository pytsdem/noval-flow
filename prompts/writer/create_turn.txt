你现在要为章节生成关键推进场景。不要解释过程，只输出场景文本。

章节标题：{chapter_title}
章节目标：{chapter_objective}
章节悬念：{chapter_cliffhanger}

premise JSON：
{premise_json}

story_blueprint JSON：
{story_blueprint_json}

chapter_plan JSON：
{chapter_plan_json}

chapter_write_brief JSON：
{chapter_write_brief_json}

scene_beat JSON：
{scene_beat_json}

characters JSON：
{characters_json}

previous_chapter JSON：
{previous_chapter_json}

已完成章节摘要串：
{completed_chapter_summary_bundle}

参考资料：
{reference_pack}

要求：
1. 这场戏必须推进局面，不是解释局面。
2. 必须至少命中一项：冲突升级、信息揭示、关系偏移、代价落下、转折发生。
3. 必须优先兑现 `scene_beat`，尤其是 `conflict`、`info_reveal`、`emotional_shift`、`end_state`。
4. 必须服从 `chapter_write_brief.must_land`，并避免 `must_avoid` 中的问题。
5. 场景结尾要为下一场或下一章留下有效推进力。
6. `story_blueprint JSON` 里的设定背景、关系网、故事线、发展线和反转，是正文推进时必须尊重的结构事实。
7. “已完成章节摘要串”加上 `previous_chapter JSON` 共同代表历史正文事实，不能写出与前文状态、关系温度、事件后果冲突的内容。
8. 直接输出场景文本。
