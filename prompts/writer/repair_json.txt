Repair the previous model output so it becomes valid JSON.

Original task summary:
{source_prompt}

Parsing error:
{error}

Text to repair:
{raw_text}

Rules:
1. Preserve the original content and structure as much as possible.
2. Only fix JSON syntax, broken nesting, missing commas, trailing commas, quote issues, and obviously malformed arrays or objects.
3. Do not add markdown. Do not explain.
4. Output exactly one JSON object that can be parsed by json.loads.