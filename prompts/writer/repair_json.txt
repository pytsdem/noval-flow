Repair the JSON so it becomes valid and matches the target schema.

Target schema:
{schema_name}

Broken JSON:
{bad_json}

Rules:
1. Output valid JSON only.
2. No markdown.
3. No explanation.
4. Do not change story meaning.
5. Do not add new plot content.
6. Fill missing fields with empty string, empty array, or default values.
7. Remove extra fields.
8. Keep arrays as arrays.
9. Keep strings as strings.
10. The final JSON must pass schema validation.
