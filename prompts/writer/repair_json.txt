请修复上一次模型输出，使其成为合法 JSON。

原始任务摘要：
{source_prompt}

解析错误：
{error}

待修复文本：
{raw_text}

要求：
1. 只修复 JSON 语法和明显的结构断裂，不要改写创意内容。
2. 保留原始任务要求的字段结构。
3. 不要输出 Markdown，不要解释，不要包裹代码块。
4. 最终只输出一个完整、可被 `json.loads` 解析的 JSON 对象。
