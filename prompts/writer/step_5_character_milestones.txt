你现在要生成“第5步：角色发展线”。不要写正文，不要输出 Markdown，只输出合法 JSON。

用户需求：
{research_query}

premise JSON：
{premise_json}

characters JSON：
{characters_json}

story_blueprint JSON：
{story_blueprint_json}

chapter_plans JSON：
{chapter_plans_json}

规划上下文 JSON：
{planning_context_json}

参考资料：
{reference_pack}

输出顶层必须包含：
- character_milestones

character_milestones 中每项必须包含：
- character_name
- axes

axes 中每项必须包含：
- axis
- phases

phases 中每项必须包含：
- phase
- label
- scenes

scenes 中每项必须包含：
- title
- trigger
- psychology
- outcome

分线结构要求（重点）：
1. 必须覆盖“所有主要角色”：凡是 role 中包含“核心角色”或“关键角色”的人物，都必须在 character_milestones 中有且仅有一条对应记录，不得漏人。
2. `character_name` 必须与角色卡中的 `name` 完全一致，不要改名、缩写或合并角色。
3. 禁止只写男女主；配角中的关键角色也必须有可执行发展线。
4. 对“核心角色”（role 含“核心角色”）必须至少输出两条主线：
- axis = 事业线
- axis = 感情线
5. 事业线建议分成多个阶段，建议命名为“第一阶段/第二阶段/第三阶段（可继续）”。
6. 感情线建议分成多个阶段，建议命名为“第一阶段/第二阶段/第三阶段/第四阶段（可继续）”。
7. 对“关键角色”也必须输出至少一条高价值发展线，建议 1-2 条（不要求固定叫法，但必须可执行）。
8. 阶段 label 必须体现该阶段状态，不得只写“推进/发展/变化”。
9. 如果输入的 `characters JSON` 只有 1 个角色，则输出中 `character_milestones` 也必须只有 1 项，且 `character_name` 必须与该角色名字完全一致，不得输出其他角色。

阶段细化要求（必须详细）：
1. 每个 phase 建议包含多个 scenes，保证阶段内容足够支撑后续写作。
2. trigger 必须是可落地事件，不是抽象概念。
3. psychology 必须写“认知变化 + 情绪变化 + 选择倾向变化”。
4. outcome 必须写“对关系/权力/身份/事业/剧情路径”的具体影响。
5. 不允许空话，如“更成熟”“更痛苦”“矛盾加深”而无机制说明。

女主优先示例要求（如果存在女主）：
1. 女主的事业线必须体现“阶段性场域迁移”（例如：王府→朝堂→地方/战后重建）。
2. 女主的感情线必须体现“阶段性关系结构变化”（例如：依赖/规训→拉扯/失控边缘→背叛/对峙→理解与放手）。
3. 女主两条线必须互相牵动：事业选择要改变感情走向，感情后果要反作用事业路径。

硬性要求：
1. 这不是人物小传，而是后续步骤与正文可直接消费的角色发展线。
2. 必须优先读取 `planning_context_json.previous_step_outputs` 的前序完整产物，并与前序结论保持一致，不得丢字段或自行改写。
3. 原始需求、角色卡、关系网、事件时间线、反转和章节规划都要共同约束这一步。
4. `axis` 优先参考 `development_axes`，但必须结合关系网、事件线、反转和章节推进重新落到可执行路径。
5. 每条发展线都要有起点、中段变化、后果扩散，不能只有情绪结论。
6. `trigger` 写触发事件，`psychology` 写认知偏移和情绪转折，`outcome` 写它改变了什么局面。
7. `outcome` 必须影响关系、身份、事业、选择或剧情路径，不能只写“更成熟”“更痛苦”。
8. 不同 axis 之间要互相牵动，不能像并列备忘录。
9. 不要生成新的角色卡、关系网、事件时间线、反转、故事线、章节信息。
10. 只输出 JSON。
11. 输出前自检：主要角色（核心+关键）不能漏；若漏了，先补齐再输出。
