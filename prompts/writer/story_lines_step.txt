你现在要生成“第7步：故事线+章节标题”。不要写正文，不要输出 Markdown，只输出合法 JSON。

用户需求：
{research_query}

风格要求：
{style_request}

当前小说 JSON：
{book_json}

当前蓝图 JSON：
{current_story_blueprint_json}

规划上下文 JSON：
{planning_context_json}

参考资料：
{reference_pack}

输出顶层必须包含：
- story_blueprint

story_blueprint 在这一步只允许包含：
- story_lines
- chapter_briefs

story_lines 中每项必须包含：
- name
- line_type
- start_state
- midpoint_shift
- end_state
- core_question

chapter_briefs 中每项必须包含：
- chapter_id
- title
- active_lines
- summary
- turn
- cliffhanger
- chapter_type
- core_question_left
- small_payoff
- reader_hook
- new_information
- relationship_shift
- ending_pull

硬性要求：
1. 原始需求与前六步产物都是硬约束，故事线和章节标题必须挂在已有结构上。
2. `planning_context_json.writing_requirements` 也是硬约束，章节标题与章节摘要必须服从全书体量、章节规模和节奏要求。
3. `story_lines` 不是事件目录换名字，而是真正持续推进全书的结构线路。
4. `line_type` 要能区分外部线、内部线、关系线、悬疑线、政治线、事业线等。
5. `chapter_briefs` 这一层优先完成“标题 + 挂线 + 基本推进”，为第8步服务。
6. 每章 `active_lines` 必须明确，让人一眼看出本章主要推进哪几条线。
7. 重要反转必须在故事线和章节标题层找到承载位置。
8. 前三章必须强制标明 `chapter_type`，只能从 `opening`、`expansion`、`first_payoff` 中选一个，分别对应第一章、第二章、第三章。
9. 前三章必须额外明确留人策略：`reader_hook`、`new_information`、`relationship_shift`、`ending_pull` 要写清。
10. 前三章还必须写清 `core_question_left` 和 `small_payoff`：前者说明这章结束后读者最想继续追问什么，后者说明这一章给了什么小兑现。
11. 第四章以后这些字段也可以继续写，但前三章必须最具体。
12. 前三章不能走“按部就班介绍世界、介绍人物、介绍设定”的常规路线，必须优先完成阅读牵引。
13. 前三章必须尽早放入强情感冲突，而且这个冲突要真的改变关系温度或角色判断，不能只是吵一句嘴。
14. `reader_hook` 不要写成抽象话术，必须回答这一章最能逼读者继续翻页的具体点是什么。
15. `relationship_shift` 不能写成“关系加深”“关系恶化”这种空话，要写清是谁对谁、因为什么、变成了什么。
16. `ending_pull` 必须尽量制造未完成的情绪、未揭开的真相、未解决的危险、未说破的关系债务。
17. 如果 `chapter_count_target` 明显较短，就不要把前三章写成过度铺垫；如果总章节较长，也不要把前三章写得像迟迟不进入正题。
18. 如果前三章仍然像常规模板，宁可让它们更锋利、更冒险，也不要写得安全、平顺、标准。
19. 如果作品偏言情、婚恋、暧昧、禁忌关系或强关系驱动，前三章与关键章节的 `reader_hook`、`relationship_shift`、`ending_pull` 必须适当体现“性张力 / 亲密张力 / 欲望压抑”。
20. 这里的“性张力”不是露骨描写，而是吸引、克制、危险感、权力错位、边界试探、误认、嫉妒、占有欲、身体感知被压住后的反噬。
21. 如果作品偏言情，至少部分 `chapter_briefs` 要明确回答：这一章里哪段关系的欲望压力变高了、谁先失守了一点、谁又把边界强行拉回去。
22. 不要生成完整章节规划。
23. 只输出 JSON。
