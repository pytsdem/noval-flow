你现在要根据用户要求修改小说构思。不要输出 Markdown，只输出合法 JSON。

修改范围：{scope}
目标对象：{target_id}
用户要求：{guidance}

当前小说 JSON：
{book_json}

参考资料：
{reference_pack}

输出规则：
1. scope = all 时，输出 title、premise、characters、chapter_plans。
2. scope = premise 时，只输出 premise。
3. scope = character 时，只输出 character。
4. scope = chapter_plan 时，只输出 chapter_plan。
5. 只能修改当前 JSON 中已经存在的部分。
6. 修改要真正响应用户要求，不是只换一下语序。
7. 只输出 JSON。