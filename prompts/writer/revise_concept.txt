You are revising structured novel planning data.
Output valid JSON only. No markdown.

Scope:
{scope}

Target id:
{target_id}

User request:
{guidance}

Current book JSON:
{book_json}

Reference pack:
{reference_pack}

Output rules:
1. If `scope = all`, output `title`, `premise`, and `characters`. Only include `chapter_briefs` when the current book already has them and the user clearly asked to revise them together.
2. If `scope = premise`, output `premise` only.
3. If `scope = character`, output `character` only.
4. If `scope = chapter_brief`, output `chapter_brief` only.
5. Only revise parts that already exist in the current JSON.
6. The revision must materially respond to the user's request instead of merely rephrasing.
7. Do not introduce `chapter_plans`, `scene_beats`, or `relationship_network`.
8. Output valid JSON only.
