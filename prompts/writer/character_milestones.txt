你现在要生成“第5步：角色发展线”。不要写正文，不要输出 Markdown，只输出合法 JSON。

用户需求：
{research_query}

premise JSON：
{premise_json}

characters JSON：
{characters_json}

story_blueprint JSON：
{story_blueprint_json}

chapter_plans JSON：
{chapter_plans_json}

规划上下文 JSON：
{planning_context_json}

参考资料：
{reference_pack}

输出顶层必须包含：
- character_milestones

character_milestones 中每项必须包含：
- character_name
- axes

axes 中每项必须包含：
- axis
- phases

phases 中每项必须包含：
- phase
- label
- scenes

scenes 中每项必须包含：
- title
- trigger
- psychology
- outcome

硬性要求：
1. 这不是人物小传，而是后续步骤与正文可直接消费的角色发展线。
2. 原始需求、角色卡、关系网、事件时间线、反转和章节规划都要共同约束这一步。
3. `axis` 优先参考 `development_axes`，但必须结合关系网、事件线、反转和章节推进重新落到可执行路径。
4. 每条发展线都要有起点、中段变化、后果扩散，不能只有情绪结论。
5. `trigger` 写触发事件，`psychology` 写认知偏移和情绪转折，`outcome` 写它改变了什么局面。
6. `outcome` 必须影响关系、身份、事业、选择或剧情路径，不能只写“更成熟”“更痛苦”。
7. 不同 axis 之间要互相牵动，不能像并列备忘录。
8. 不要生成新的角色卡、关系网、事件时间线、反转、故事线、章节信息。
9. 只输出 JSON。
